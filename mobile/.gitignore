# Expo / RN build + local artifacts
node_modules/
.expo/
dist/
web-build/
*.orig.*

# Native projects generated by prebuild (we use managed workflow + EAS)
/ios
/android

# Metro
.metro-health-check*

# Debug / env
npm-debug.*
*.log
.env*.local
